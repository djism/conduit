global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: conduit
    static_configs:
      # host.docker.internal resolves to the host machine from inside Docker.
      # This lets Prometheus reach our Go server running on the host.
      - targets: ["host.docker.internal:8080"]